#include <stdio.h>

int main() {
    // 여기에 코드를 작성해주세요.
    while(1)
    {
        int n;
        scanf(" %d", &n);
        if(n == 0)
        {
            break;
        }
        printf("%d\n", n);
    }
    return 0;
}